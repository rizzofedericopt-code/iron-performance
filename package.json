{
  "name": "iron-performance",
  "version": "1.0.0",
  "private": true,
  "description": "Monitoraggio dei test di squadra nel tempo — PWA + API serverless su Vercel",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0"
  },
  "scripts": {
    "test": "node test/roundtrip.mjs && node test/merge.mjs && node test/anamnesi.mjs && node test/annulla.mjs && node test/concorrenza.mjs"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.2.17"
  }
}
